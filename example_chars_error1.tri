# This is an example program
let
	const MAX_NUM ~ 10;
	var n: Integer
in
	begin
		put('a'); put('b'); put('? '); 
		getint(var n);
		if (n > 0) then
			if (n < MAX_NUM) then
				while (n > 0) do
					begin
						putint(n); puteol();
					end
			endif
		endif
	end
