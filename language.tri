# This is a comment
# This is a comment
let
	const MAX_VAL ~ 10;
	var n: Integer
in
	begin
		getint(var n);
		if n > 0 then
			if n < MAX_VAL then
				while n > 0 do
					begin
						putint(n); puteol();
						n := n-1
					end
			else
		else
	end
