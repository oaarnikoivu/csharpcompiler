# This is my example programd
let 
	const myNum ~ 10;
	var n: Integer
in
	begin
		getint(var n);
		if n > 0 then
			if n < myNum then
				for i := 0 to 10 do
					begin
						putint(n); puteol();
						n := n * i 
					end
				next 
			endif
		endif
	end	 
