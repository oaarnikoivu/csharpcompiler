# This is an example program using a for loop
let
	const MAX ~ 10;
	var n: Integer;
	var i: Integer
in
	begin
		getint(var n);
		getint(var i);
		for i := 0 to MAX do
			begin
				putint(n); puteol();
				n := n * i
			end
		next
	end
